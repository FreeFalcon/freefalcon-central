// Artscout - 2026 (#104, Linux Ф1): <wtypes.h> -- the COM base typedefs. windows.h + win32_com.h already define
// them here, so this exists only so the #include resolves.
#ifndef FF_WIN32SHIM_WTYPES_H
#define FF_WIN32SHIM_WTYPES_H
#include <windows.h>
#endif
